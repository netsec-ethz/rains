version: 3
jobs:
  build:
    docker:
      - image: scionproto/scion_base
        environment:
            GOPATH=/tmp/go
    working_directory: /tmp/go/src/github.com/netsec-ethz/rains
    steps:
      - checkout
      - restore_cache:
          keys:
            - v3-pkg-cache
      - run: go get -u github.com/golang/dep/cmd/dep
      - run:
          name: scion,
          command: |
            mkdir -p $GOPATH/src/github.com/scionproto/scion
            git clone https://github.com/scionproto/scion $GOPATH/src/github.com/scionproto/scion
            cd $GOPATH/src/github.com/scionproto/scion
            ./env/deps
      - run:
          name: run build
          command: |
            dep ensure
            go build -v cmd/rainsd/rainsd.go
            go build -v cmd/rainsdig/rainsdig.go
            go build -v cmd/zonepub/zonepub.go
      - run:
          name: run tests
          command: |
            go fmt ./...
            go vet ./internal/...
            go vet ./examples/...
            go vet ./cmd/...
            go vet ./test/...
            go test -v ./test/integration/
      - save_cache:
          key: v3-pkg-cache
          paths:
            - "/tmp/go/pkg"
